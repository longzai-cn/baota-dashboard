import PageContainer from "@/common/PageContainer";
import { Empty } from "antd";

const LogsPage: React.FC = () => {
  return (
    <PageContainer name="日志">
      <Empty description="Building..." />
    </PageContainer>
  );
};

export default LogsPage;
