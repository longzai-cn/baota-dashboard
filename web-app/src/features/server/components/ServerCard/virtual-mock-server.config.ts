import { Server } from "@/features/server/common/server.entity";

export const VIRTUAL_MOCK_SERVER: Server = {
  id: "mock-server",
  name: "Virtual Mock Server",
  host: "http://localhost:5173/api",
  key: "",
}